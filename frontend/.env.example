# Copy this file to .env.local for local development (Vite auto-loads .env.local
# and it's already git-ignored, so real secrets never get committed).

# Base URL of the backend API (Auth/Identity, Consent, Record Management services)
VITE_API_BASE_URL=http://localhost:8000/api
