import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.migranthealth.record',
  appName: 'Portable Health Record',
  webDir: 'dist'
};

export default config;
