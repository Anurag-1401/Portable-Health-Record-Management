// Must stay in sync with the `fhir_resource_type` enum in schema.sql.
export const FHIR_RESOURCE_TYPES = {
  PATIENT: 'Patient',
  OBSERVATION: 'Observation',
  CONDITION: 'Condition',
  MEDICATION_REQUEST: 'MedicationRequest',
}
